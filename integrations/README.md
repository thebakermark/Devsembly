# Integrations

Each integration directory will contain adapters, configuration schemas, health checks and least-privilege permission documentation. Integrations must fail closed when credentials or policy are invalid.
